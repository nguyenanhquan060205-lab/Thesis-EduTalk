[pytest]
testpaths = tests
addopts = --strict-markers --tb=short
# Tắt cảnh báo của thư viện ngoài cho đỡ loãng; cảnh báo của chính app thì giữ.
filterwarnings =
    ignore::DeprecationWarning:google.*
    ignore::DeprecationWarning:pydantic.*
